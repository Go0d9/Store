<div class="jumbotron">
  <% @posts.each do |post| %>
      <h1><%= post.title %></h1>
      <p><%= truncate(strip_tags(post.description), lenght: 80) %></p>
      <%= link_to 'Learn more', post_path(post), :class => 'btn btn-primary btn-lg' %>
  <% end %> 
</div>